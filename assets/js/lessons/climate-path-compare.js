(function(){'use strict';const canvas=document.getElementById('climate-p3-chart');if(!canvas)return;const ctx=canvas.getContext('2d'),status=document.getElementById('climate-p3-status');const paths={preindustrial:{name:'Pre-industrial reference',end:.2,color:'#9fb7c8',unc:.18},today:{name:'Current trajectory',end:2.7,color:'#ffb454',unc:.45},paris:{name:'Paris-aligned',end:1.55,color:'#59d4ff',unc:.28},bau:{name:'High emissions',end:4.4,color:'#ff667d',unc:.7}};let selected='today';
function curve(end,t){const lag=1-Math.exp(-t*2.1);return 1.2+(end-1.2)*lag}
function draw(){const w=canvas.width,h=canvas.height,p=paths[selected],pad=42;ctx.clearRect(0,0,w,h);ctx.fillStyle='#040b18';ctx.fillRect(0,0,w,h);ctx.strokeStyle='rgba(180,215,255,.15)';ctx.fillStyle='#b9ccdf';ctx.font='12px system-ui';for(let i=0;i<=5;i++){const y=pad+(h-pad*2)*i/5;ctx.beginPath();ctx.moveTo(pad,y);ctx.lineTo(w-pad,y);ctx.stroke();ctx.fillText((5-i)+'°C',6,y+4)}for(let i=0;i<=4;i++)ctx.fillText(2020+i*20,pad+(w-pad*2)*i/4-14,h-12);ctx.beginPath();for(let i=0;i<=80;i++){const t=i/80,x=pad+t*(w-pad*2),y=h-pad-curve(p.end,t)/5*(h-pad*2)-p.unc/5*(h-pad*2);i?ctx.lineTo(x,y):ctx.moveTo(x,y)}for(let i=80;i>=0;i--){const t=i/80,x=pad+t*(w-pad*2),y=h-pad-curve(p.end,t)/5*(h-pad*2)+p.unc/5*(h-pad*2);ctx.lineTo(x,y)}ctx.closePath();ctx.fillStyle=p.color+'33';ctx.fill();ctx.beginPath();for(let i=0;i<=80;i++){const t=i/80,x=pad+t*(w-pad*2),y=h-pad-curve(p.end,t)/5*(h-pad*2);i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.strokeStyle=p.color;ctx.lineWidth=4;ctx.stroke();status.textContent=p.name+': illustrative 2100 anomaly '+p.end.toFixed(1)+'°C, uncertainty ±'+p.unc.toFixed(2)+'°C. Ocean lag bends the line instead of jumping instantly.'}
document.getElementById('climate-p3-actions').addEventListener('click',e=>{const b=e.target.closest('[data-climate-path]');if(!b)return;selected=b.dataset.climatePath;document.querySelectorAll('[data-climate-path]').forEach(x=>x.setAttribute('aria-pressed',String(x===b)));draw()});draw();}());
